package com.example.firstprototype.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * PestaShare Premium Color Palette
 * Defines the core colors used throughout the application to maintain brand consistency.
 */

// --- Brand Colors (Vibrant yet professional) ---
val PestaGreen = Color(0xFF10B981)       // Modern emerald green for success, balances, and sustainability
val PestaBlue = Color(0xFF3B82F6)        // Premium electric blue for primary action buttons
val PestaGreenDark = Color(0xFF047857)   // Dark green for text on light backgrounds
val PestaBlueDark = Color(0xFF1D4ED8)    // Deep blue for focused states

// --- Text Hierarchy and Dark Containers ---
val TextPrimary = Color(0xFF0F172A)      // Slate 900: Dark blue-black for main titles
val TextSecondary = Color(0xFF475569)    // Slate 600: Blue-grey for descriptions and subtitles
val TextMuted = Color(0xFF94A3B8)        // Slate 400: Light grey for placeholders and secondary text

// --- Backgrounds and Organic Surfaces ---
val BackgroundSurface = Color(0xFFF8FAFC) // Slate 50: Ultra-clean background for better readability
val CardSurface = Color(0xFFFFFFFF)       // Pure white for floating cards
val BorderLight = Color(0xFFE2E8F0)       // Slate 200: Subtle borders for input fields